apiVersion: v1
kind: ResourceQuota
metadata:
  name: payments-budget
  namespace: payments
  labels:
    app.kubernetes.io/part-of: p2-w10-lab
spec:
  hard:
    pods: "5"
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi

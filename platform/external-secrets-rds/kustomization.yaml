apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: demo
resources:
  - secret-store.yaml
  - external-secret.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: rds-managed-secret-ref
    literals:
      - secretArn=arn:aws:secretsmanager:ap-southeast-1:193229476041:secret:p2-w10-lab-lab/rds-LnIe4O
replacements:
  - source:
      kind: ConfigMap
      name: rds-managed-secret-ref
      fieldPath: data.secretArn
    targets:
      - select:
          kind: ExternalSecret
          name: web-db-secret
        fieldPaths:
          - spec.data.[secretKey=host].remoteRef.key
          - spec.data.[secretKey=port].remoteRef.key
          - spec.data.[secretKey=database].remoteRef.key
          - spec.data.[secretKey=username].remoteRef.key
          - spec.data.[secretKey=password].remoteRef.key
